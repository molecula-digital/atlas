import { NotFoundContent } from '@/components/NotFoundContent'

/** Uses the frontend route-group layout, which already supplies site chrome. */
export default function FrontendNotFound() {
  return <NotFoundContent />
}
